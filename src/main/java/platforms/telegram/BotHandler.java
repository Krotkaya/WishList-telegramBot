package platforms.telegram;
import logic.RequestHandler;

public class BotHandler {
    public static String handleMessage(String message) {
        // Логика обработки сообщения (здесь просто возвращаем то же самое)
        return message;
    }
}
